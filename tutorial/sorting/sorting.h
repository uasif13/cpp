// ifndef - Macro has not been previously defined
#ifndef SORTING_H
#define SORTING_H

int * insert_sort(int*& new_arr, const int*& old_arr, const int length);
void print_arr(const int*& arr, const int length);

#endif SORTING_H